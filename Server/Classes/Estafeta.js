const Utilizador = require("./Utilizador.js");

class Estafeta extends Utilizador {
    constructor(Estado) {
        super(Id, Password, Nome, Email, Contacto)
        this.estado = Estado;
    }
}

module.exports = Estafeta;